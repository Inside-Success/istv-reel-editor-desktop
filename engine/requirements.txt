# Minimal deps for the export + camera-sync engine this desktop app shells out to
# (export_cli.py, sync_cameras_cli.py). Not the full pipeline's requirements.txt —
# no rev-ai/anthropic/python-dotenv here, since transcription and reel selection
# happen against the hosted backend, not locally.
numpy>=1.24.0
python-docx>=1.1.0
Pillow>=10.0.0
